apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "botblocker.fullname" . }}-test-connection"
  labels:
    {{- include "botblocker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "botblocker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
