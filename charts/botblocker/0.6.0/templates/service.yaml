apiVersion: v1
kind: Service
metadata:
  name: {{ include "botblocker.fullname" . }}
  labels:
    {{- include "botblocker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "botblocker.selectorLabels" . | nindent 4 }}
