apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "freelight-landing.fullname" . }}-test-connection"
  labels:
    {{- include "freelight-landing.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "freelight-landing.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
