apiVersion: v1
kind: Service
metadata:
  name: {{ include "freelight-docs.fullname" . }}
  labels:
    {{- include "freelight-docs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "freelight-docs.selectorLabels" . | nindent 4 }}
