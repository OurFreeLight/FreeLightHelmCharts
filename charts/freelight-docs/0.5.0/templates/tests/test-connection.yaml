apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "freelight-docs.fullname" . }}-test-connection"
  labels:
    {{- include "freelight-docs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "freelight-docs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
